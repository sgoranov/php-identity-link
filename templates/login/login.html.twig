{% extends 'base.html.twig' %}

{% block content %}
    <h1>Login</h1>
    {% if error %}
        <div class="alert alert-danger" role="alert">
            {{ error.getMessage() }}
        </div>
    {% endif %}
    {{ form(form) }}
{% endblock %}